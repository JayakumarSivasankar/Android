pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = "NativewithUnityAppv1"
include ':app'
include ':unityLibrary'
project(':unityLibrary').projectDir = new File('C://Users//Jayakumar//Testprojectv1//androidBuild5//unityLibrary')

//include ":unityLibrary:FirebaseApp.androidlib"
//project(":unityLibrary:FirebaseApp.androidlib").projectDir = file("./unityLibrary/FirebaseApp.androidlib")

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        flatDir {
            dirs "${project(':unityLibrary').projectDir}/libs"
        }
    }
}